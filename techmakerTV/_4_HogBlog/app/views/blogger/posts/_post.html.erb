<!-- app/views/posts/_post.html.erb -->
<tr>
  <td><%= link_to post.title, blogger_post_path(post) %></td>
  <td></td>
  <td><%= link_to 'Edit', edit_blogger_post_path(post), class: 'btn btn-sm btn-warning' %></td>
  <td><%= link_to 'Remove', blogger_post_path(post), class: 'btn btn-sm btn-danger', method: :delete, data:{confirm: "Are you sure ?"} %></td>
</tr>